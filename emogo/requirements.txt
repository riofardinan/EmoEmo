# Verified against torch 2.13.0+cpu / transformers 5.14.1 on Python 3.14.
# On the server, install the CUDA build of torch that matches your driver:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.1
transformers>=4.30
tokenizers>=0.13
pandas>=1.3
numpy>=1.21
scikit-learn>=1.0
